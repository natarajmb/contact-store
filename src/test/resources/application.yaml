spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:contact_store;INIT=CREATE SCHEMA IF NOT EXISTS contact_store\\;SET SCHEMA contact_store\\;"
    username: sa
    password:

  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true